/* ============================================================
   숨비 (SUMBI) — audio-data.js
   base64 인라인 SFX.

   ⚠ 왜 base64 인가
     file:// 에서 fetch / XMLHttpRequest 는 차단되므로
     decodeAudioData 에 넘길 바이트를 얻을 방법이 base64 → atob 뿐입니다.
     (audio.js 의 decodeBase64Audio 참조)

   ⚠ 현재 상태 — 전 항목이 비어 있습니다.
     비어 있는 키는 audio.js 의 절차 생성본(PROC)으로 자동 폴백되므로
     지금 상태로도 소리는 전부 납니다.
     녹음본을 넣으면 그 키만 자동으로 base64 쪽이 우선합니다.

   ★ 숨비소리는 이 게임의 로고입니다. 반드시 직접 녹음하세요.
     외부 영상·다큐에서 추출하는 것은 절대 금지입니다.

   넣는 법
     1) 44.1kHz 모노 wav/mp3 로 짧게 (0.3~1.5초) 자릅니다
     2) base64 로 변환합니다
          macOS:  base64 -i sumbi.wav | tr -d '\n' | pbcopy
     3) 아래 해당 키의 빈 문자열 자리에 붙여넣습니다
     4) 용량 주의 — 6개 합계 300KB 이하를 권장합니다
        (앰비언스·심박·기포는 절차 생성이므로 용량 0)
   ============================================================ */
var SUMBI = SUMBI || {};

var AUDIO_DATA = {
  sumbi:     '',   /* 숨비소리 — 출수 순간. 이 게임의 시그니처 */
  harvest:   '',   /* 채집 완료(잡는 순간의 임팩트) */
  bank:      '',   /* 테왁에 비움 */
  sell:      '',   /* 판매 */
  upgrade:   '',   /* 장비 구매 */
  rank:      '',   /* 승급 */
  fail:      '',   /* 물숨 (실패) */
  hv_stage2: '',   /* 채집 저항 — 버티기 구간 진입 틱 */
  hv_stage3: '',   /* 채집 저항 — 마무리 구간 진입 틱 */
  hv_slip:   '',   /* 채집 저항 — 미끄러짐 */
  bagpop:    ''    /* 채집물이 망사리에 도착 */
};
